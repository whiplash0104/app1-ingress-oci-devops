version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash      
steps:
  - type: Command
    timeoutInSeconds: 1200
    name: "Build container image"
    command: |
      cd ${OCI_WORKSPACE_DIR}/Docker
      docker build -t ingressappokedev:v1.0  .
outputArtifacts:
 - name: myapp-image
  type: DOCKER_IMAGE
  location: ingressappokedev:v1.0
